fastapi==0.110.0
uvicorn[standard]==0.27.1
SQLAlchemy==2.0.25
alembic==1.13.1
pydantic==1.10.15
python-multipart==0.0.9
passlib[bcrypt]==1.7.4
# Passlib 1.7.4 is not yet compatible with bcrypt>=4.0, so we pin a
# supported version explicitly to avoid runtime errors when hashing
# passwords.
bcrypt>=3.2,<4
PyJWT==2.8.0
requests==2.31.0
APScheduler==3.10.4
email-validator==2.1.1
python-dotenv==1.0.1
